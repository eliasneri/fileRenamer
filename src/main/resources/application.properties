db.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/
db.dataname=${DB_NAME:database}
db.user=${DB_USER:postgres}
db.password=${DB_PASSWORD:123456789}
schema=${DB_SCHEMA:migration}
folder.name=${FOLDER_NAME:folder}